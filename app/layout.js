import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title:
    "Sherise Sale Week by Yuukke | Exclusive Launch Offers & Limited-Time Deals",
  description:
    "Experience Sherise Sale Week by Yuukke — a limited-time celebration of elegance, curated hampers, and exclusive launch pricing. Shop premium collections before the countdown ends.",
};
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
